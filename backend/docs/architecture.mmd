%% Legacy single-file diagram — prefer the split sources:
%%   architecture-system.mmd  — venue ↔ backend ↔ UI
%%   architecture-tick.mmd      — engine trading path
%%   architecture-events.mmd    — EventBus fan-out
%%
%% This file keeps a compact end-to-end view for tools that only load one .mmd.

flowchart TB
    subgraph External
        BN[Binance WS + REST]
        UI[React console]
    end

    subgraph Process["backend/ — one asyncio loop"]
        GW[Gateway]
        subgraph Engine
            MD[Market data] --> FS[Features] --> ST[Strategy]
            ST --> RK[Risk] --> EX[Execution] --> OMS[OMS]
        end
        BUS[EventBus]
        API[FastAPI]
        GW <--> MD
        OMS --> GW
        GW -->|fills| RK
        Engine --> BUS
        BUS --> API
    end

    BN <--> GW
    API <--> UI
