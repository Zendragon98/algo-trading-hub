"""Interactive Brokers adapter (skeleton).

Drop-in template for a second venue. Wire up the real `ib_async` (or
`ib_insync`) calls in each method to bring it online; the engine will
not need any changes.
"""
